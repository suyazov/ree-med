# ТриМед — WordPress тема

## Общая информация

- **Проект:** ТриМед (trimed)
- **Сайт:** https://test1.sy3.ru
- **Тип:** Landing page "Дезинфекция" на WordPress
- **WordPress:** установлен в `/var/www/test1.sy3.ru`
- **Тема:** `trimed` в `/var/www/test1.sy3.ru/wp-content/themes/trimed/`
- **Главная страница:** страница "Дезинфекция" (шаблон `page-disinfection.php`)

## Серверное окружение

- **Web-сервер:** Nginx
- **PHP:** 8.4 FPM
- **База данных:** MariaDB
- **БД:** `test1_sy3_ru`
- **Пользователь БД:** `test1_sy3_ru`
- **SSL:** Let's Encrypt (действителен до 2026-09-15)

## Доступы

- **Админка WordPress:** https://test1.sy3.ru/wp-admin
- **Логин:** `admin`
- **Пароль:** `ijNIa8AgofU3Z5AfBGia`

## Используемые плагины

- **Advanced Custom Fields (ACF)** — обязательно активирован. Все редактируемые блоки страницы настроены через `inc/acf-fields.php`.
- **Classic Editor** — активирован, чтобы на странице "Дезинфекция" использовался классический редактор вместо блокового.

## Типографика

- **Основной шрифт:** Exo 2 (Google Fonts)
- **Дополнительный шрифт:** HelveticaNeueCyr (fallback)
- **Начертания:** 300, 400, 500, 600, 700, 800

## Брендовые цвета

| Цвет | HEX | Использование |
|------|-----|---------------|
| Тёмно-зелёный | `#315046` | Основной акцент, заголовки |
| Зелёный | `#367643` | Кнопки, ссылки |
| Светло-зелёный | `#3F8D50` | Градиенты, карточки |
| Ярко-зелёный | `#51A462` | Градиенты, акценты |
| Светло-серый | `#EFEFEF` | Фоны |
| Белый | `#F9F9F9` | Фоны карточек |
| Серый | `#E6E7E8` | Декоративные элементы |
| Чёрный | `#353535` | Текст |

## Структура темы

```
trimed/
├── style.css              # Стили темы WordPress
├── functions.php          # Подключение скриптов, меню, ACF
├── index.php              # Базовый шаблон
├── page-disinfection.php  # Шаблон лендинга "Дезинфекция"
├── header.php             # Шапка сайта
├── footer.php             # Подвал с картой и контактами
├── screenshot.png         # Скриншот темы (если добавлен)
├── inc/
│   └── acf-fields.php     # Регистрация ACF-полей
├── assets/
│   ├── css/main.css       # Основные стили
│   ├── js/main.js         # FAQ-аккордеон, мобильное меню, AJAX-форма
│   └── img/               # Изображения из Figma
└── template-parts/        # Переиспользуемые части шаблонов
```

## Разделы лендинга

1. **Hero** — заголовок, описание, преимущества, CTA, изображение, бейджи.
2. **Кому подходит** — сетка карточек с аудиториями, каждая карточка имеет стрелку ↗ в левом нижнем углу.
3. **Что мы поставляем** — круговая диаграмма с позициями (на мобильных превращается в список).
4. **Что входит** — карточки этапов работы.
5. **Задачи клиентов** — зелёный блок + список задач.
6. **Почему выбирают ТриМед** — преимущества, статистика, склад.
7. **Реализованные проекты** — карточка проекта.
8. **Партнёры** — карточки производителей.
9. **FAQ** — аккордеон вопросов/ответов.
10. **Форма заявки** — форма обратной связи с AJAX-отправкой.
11. **Footer** — карта (статичное изображение) + карточка с контактами.

## Редактирование контента

Всё содержимое страницы редактируется через админку WordPress:

1. Перейти в **Страницы → Дезинфекция**.
2. Внизу классического редактора отобразятся поля ACF, сгруппированные по вкладкам (Hero, Кому подходит, Что мы поставляем, Что входит, Задачи клиентов, Почему выбирают, Проекты, Партнёры, FAQ, Форма заявки).
3. В каждой вкладке доступны заголовки, описания и repeater-поля соответствующей секции.
4. Внести изменения и нажать **Обновить**.

### ACF Repeater-поля

- `hero_badges` — бейджи в hero.
- `hero_features` — преимущества hero (иконка + текст).
- `audience_cards` — карточки аудиторий (название, изображение, стиль).
- `supplies_items` — позиции поставок.
- `included_cards` — карточки "Что входит" (изображение, номер, заголовок, флаг зелёной карточки).
- `tasks_list` — список задач (иконка + текст).
- `why_features` — преимущества в секции "Почему выбирают".
- `partners` — партнёры (логотип, название, описание).
- `faq_items` — вопросы/ответы (флаг "открыт по умолчанию").

Если repeater-поле не заполнено, на сайте выводится fallback-контент по умолчанию.

## Меню

- **Главное меню** зарегистрировано в расположении `primary`.
- Редактируется в **Внешний вид → Меню**.
- Созданы страницы: Главная, Каталог, Услуги, Проекты, Контакты.
- В шапке выводятся иконки: сравнение/график, избранное, личный кабинет/магазин, поиск.
- На мобильных устройствах отображается бургер-меню.

## Скрипты

- `assets/js/main.js` содержит:
  - FAQ-аккордеон.
  - Переключение мобильного меню.
  - AJAX-отправку формы заявки.

## Форма заявки

- Форма отправляется через AJAX на `admin-ajax.php`.
- Обработчик: `trimed_handle_contact_form` в `functions.php`.
- Письмо отправляется на email администратора сайта (`get_option('admin_email')`).
- Для надёжной доставки рекомендуется настроить SMTP (например, через плагин).

## Ресурсы

- **Источник дизайна:** Figma-файл "ТРИМЕД-for-AS"
- **Десктопный фрейм:** `143:12` (1440px)
- **Мобильный фрейм:** `164:557` (375px)
- **Изображения:** скачаны через Figma API, являются placeholder'ами, подлежат замене на финальные.

### Figma API

- **Токен:** хранится в файле `.env` (не коммитится в репозиторий).
- **Ключ файла:** заполните `FIGMA_FILE_KEY` в `.env` (берётся из URL Figma-файла после `figma.com/design/`).
- **Node ID для Hero (desktop):** `143:12`.
- **Node ID для мобильной версии:** `164:557`.

Пример запроса к Figma API:
```bash
curl -H "X-Figma-Token: $FIGMA_API_KEY" \
  "https://api.figma.com/v1/files/$FIGMA_FILE_KEY/nodes?ids=143:12"
```

## Важные замечания

- Все фото и тексты — мокапы. Клиенту нужно будет заменить их на реальные.
- Карта в футере — статичное изображение `footer-map.png`. При необходимости заменить на интерактивную (Яндекс/Google Maps).
- Иконки соцсетей в футере — placeholder'ы до подтверждения мессенджеров.
- Тема использует кэширование версий CSS/JS через константу `TRIMED_VERSION` в `functions.php`. При каждом изменении стилей/скриптов рекомендуется увеличивать версию.

## Последние правки (Hero vs Figma)

- Подключён Figma API, получены точные размеры и цвета из фрейма `143:12`.
- Hero сверен с макетом: ширина колонок 682 px / 594 px, gap 68 px, отступ сверху 20 px.
- Тексты и шрифты Hero приведены к Figma: заголовок 56 px, описание 20 px/24 px, карточки преимуществ 14 px с точными ширинами.
- Бейджи: фон `rgba(54,118,67,0.5)` + blur 20 px, layout 2+1, точные размеры и padding.
- Кнопки: CTA с зелёным градиентом, "Получить консультацию" — серый градиент + blur 60 px.
- Шапка: фон возвращён на `#E6E7E8`, иконки действий приближены к макету.
- Мобильная версия Hero адаптирована под изменения.
- Иконки действий в шапке приближены к макету (график, избранное, дом, поиск).

## Последние правки (шапка + Hero)

- **Шапка (header):** полностью переверстан блок `.header-actions` под Figma:
  - иконки без круглой подложки: сравнение (4 столбца), избранное (`mdi:heart-outline`), корзина (`mingcute:basket-fill`), поиск (`mi:search`);
  - счётчики — серые квадраты 24×24 (desktop) / 20×20 (mobile), `border-radius: 4px`, фон `#D1D3D4`, текст `#315046`;
  - расстояния: сравнение↔избранное 16 px, избранное↔корзина 40 px, корзина↔поиск 16 px;
  - мобайл: сердце + корзина + бургер, без круглых подложек.
- **Hero:**
  - третий бейдж «Эффективная система инфекционного контроля» приведён к макету: ширина 385 px, прижат к правому краю, `font-size: 14px`;
  - карточка «Получить консультацию» теперь в одну строку, `font-weight: 400`, позиционирована 28 px от правого и нижнего края изображения;
  - на мобильном третий бейдж больше не наследует лишний `font-size: 16px`.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.2.2`.

## Последние правки (типографика и Hero)

- **Заголовок H1:** слово «для» теперь не отрывается от «дезинфекции» (`.hero-title-line { white-space: nowrap; }`), убран принудительный перенос перед «контроля» — заголовок занимает 3 строки как в Figma.
- **Описание и бейджи:** явно задан `font-weight: 400` для `.hero-desc` и `.hero-badge`.
- **Карточки преимуществ:** иконки приведены к высоте 32 px с автоматической шириной (Figma — 23–35×32 px), `line-height: 120%`, текст в карточках теперь одной строкой на десктопе.
- **Бейджи поверх изображения:** убран `backdrop-filter: blur(20px)`, оставлена только полупрозрачная заливка `rgba(54,118,67,0.5)`.
- **Кнопка «Получить консультацию»:** убран `backdrop-filter: blur(60px)`, оставлен серый градиент.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.2.3`.

## Последние правки (блок «Кому подходит»)

- **Заголовок секции:** слова «разных направлений» выделены зелёным через `<span class="text-green">`; класс `.text-green` привязан к `--color-green`.
- **Стрелки карточек:** убран белый круглый фон, оставлена только SVG-стрелка 24×24 (20×20 на мобильном), цвет наследуется от карточки (тёмный на светлых, белый на зелёных/фото).
- **Фото-карточки:** оверлей для всех `.has-image` теперь зелёный полупрозрачный `rgba(54,118,67,0.5)` (как в Figma).
- **Скругления и цвета:** `border-radius: 12px` и фон серой карточки `#E6E7E8` соответствуют макету.
- **Типографика карточек:** `font-size: 20px`, `line-height: 110%`, `font-weight: 600` — по Figma.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.2.4`.

## Последние правки (Hero — доработки по обратной связи)

- **Подключение шрифта:** к Google Fonts URL добавлен `subset=cyrillic`, чтобы кириллический текст рендерился шрифтом Exo 2 нужного начертания (Regular/400) вместо системного fallback'а.
- **Описание под H1:** явно задан `font-weight: 400`.
- **Иконки в карточках преимуществ:** уменьшены с 32 px до 28 px по высоте (ширина auto с сохранением пропорций).
- **Отступы между карточками преимуществ:** `gap` уменьшен с 12 px до 10 px.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.2.5`.

## Последние правки (блок «Что мы поставляем»)

- **Заголовок секции:** слова «инфекционного контроля» выделены зелёным через `<span class="text-green">`.
- **Круговая диаграмма:**
  - добавлена гарантированная подстановка 11 позиций (если в ACF меньше 11);
  - позиции элементов пересчитаны по координатам Figma относительно центра диаграммы (600×489 px);
  - центр изображения и концентрические кольца привязаны к тем же координатам.
- **Точки-маркеры:** вынесены за пределы фона подписи, размер 20×20 px, обводка `rgba(54,118,67,0.5)` 2.8 px (как в Figma).
- **Подписи элементов:** фон `#EFEFEF`, скругление 12 px, отступ 12×16 px; шрифт 16 px / 110% / 400.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.2.6`.

## TODO / Возможные доработки

- [ ] Заменить placeholder-изображения на финальные.
- [ ] Добавить проекты в ACF repeater с полноценным слайдером.
- [ ] Заменить статичную карту в футере на интерактивную.
- [ ] Настроить SMTP для отправки писем из формы.
- [ ] Заменить placeholder-иконки соцсетей в футере на реальные.
- [ ] Оптимизация изображений и Core Web Vitals.

## Последние правки (заголовки секций)

- **Hero (H1):**
  - добавлен принудительный перенос после «решения» и перед «и», чтобы заголовок занимал ровно 3 строки;
  - слово «для» в строке «для дезинфекции» теперь наследует тёмный цвет заголовка, а зелёным выделено только «дезинфекции»;
  - убран лишний общий селектор `.hero-title span`, зелёный цвет и `font-weight: 600` применяются только к `.text-green`.
- **Блок «Что входит»:** слова «инфекционного контроля» в заголовке выделены зелёным через `<span class="text-green">`.
- **Блок «Почему выбирают ТриМед»:** зелёным выделена вторая часть заголовка — «для медицинских учреждений региона».
- **Блок «Партнёры»:**
  - заголовок получил базовое начертание 400 (`.partners-title`), как в Figma;
  - фраза «с ведущими» обёрнута в `<strong>` с начертанием 600;
  - слово «производителями» выделено зелёным.
- **FAQ:** слово «вопросы» в заголовке выделено зелёным.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.3.1`.

## Последние правки (футер / карта)

- **Карта:** оставлена статичной (`assets/img/footer-map.png`) в бело-серых тонах с наложенным полупрозрачным белым слоем.
- **Иконка адреса:** добавлена CSS-анимация пульсации для `.map-pulse` (`@keyframes map-pulse`) — два внешних круга масштабируются и растворяются, центральная точка остаётся неподвижной.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.3.2`.

## Последние правки (детальная проверка по чек-листу)

- **Hero:** иконки в карточках преимуществ уменьшены с 28 px до 24 px; описание оставлено Regular 400.
- **Audience («Работаем с учреждениями…»):**
  - `border-radius` карточек уменьшен с 12 px до 8 px;
  - зелёный оверлей на фото-карточках усилен до `rgba(54, 118, 67, 0.65)`;
  - стрелки реализованы как простые SVG без фона.
- **Все зелёные акценты в заголовках** проверены и на месте:
  - Hero: «дезинфекции»
  - Audience: «разных направлений»
  - Supplies: «инфекционного контроля»
  - Included: «инфекционного контроля»
  - Why-us: «для медицинских учреждений региона»
  - Projects: весь заголовок зелёный
  - Partners: «производителями»
  - FAQ: «вопросы»
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.3.3`.

## Последние правки (по детальному ТЗ с факт-чеком Figma API)

- **Hero:** иконки в карточках преимуществ оставлены 24 px по запросу (в bounding-box Figma 23×32 px, но визуально уменьшены для лучшего восприятия).
- **Audience («Работаем с учреждениями…»):**
  - `border-radius` возвращён к 12 px (Figma API: `Rectangle 28` `cornerRadius: 12.0`);
  - оверлей на фото-карточках возвращён к `rgba(54, 118, 67, 0.5)` (Figma API: overlay `opacity: 0.5`).
- **Supplies («Все для организации…»):**
  - точки-маркеры оставлены 20 px (Figma API: `Ellipse 11` 20×20 px);
  - центральное изображение 304 px (Figma API: `Ellipse 8` 304×304 px);
  - фоновые кольца оставлены `border: 1px solid rgba(54, 118, 67, 0.2)` (Figma API: `strokeOpacity: 0.2`).
- **Зелёные акценты, стрелки без кругов, подзаголовок Hero 400** — подтверждены и на месте.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.3.4`.

## Последние правки (блок «Почему выбирают ТриМед» / «Надёжный поставщик»)

- **Сетка:** перестроена в два ряда по ТЗ проверки блока:
  - верхний ряд — левая фото-карточка и фото склада (одинаковая высота 187 px);
  - нижний ряд — центральный белый блок и список преимуществ под фото склада;
  - левая нижняя ячейка оставлена пустой по согласованию.
- **Центральный блок:** уменьшена высота, убраны лишние вертикальные отступы, текст «Работа с государственными и частными учреждениями» прибит к верху (`align-items: flex-start`).
- **Зелёный плюсик:** уменьшен с 28 px до 24 px, позиция 16 px от верхнего/правого края.
- **Список справа:** уменьшены `gap` и `padding` пунктов, убран `min-height` для компактности.
- **Бейджи:** оставлены в одном ряду внизу левой фото-карточки (`flex-wrap: nowrap`).
- **Адаптивность:** планшетный и мобильный breakpoints сбрасывают `grid-row/grid-column` в `auto`, чтобы элементы шли в потоке.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.3.6`.

## Последние правки (блок «Что мы поставляем» / «Все для организации инфекционного контроля»)

- **Точки-маркеры:** изменены на пустые кольца 10×10 px с обводкой 2 px `#3A7D44` (прозрачный фон).
- **Отступ между точкой и текстом:** `gap` уменьшен до 8 px.
- **Карточки с текстом:** `padding: 6 px 12 px`, `border-radius: 8 px`, фон `#F5F5F5`, шрифт 14 px / 400 / `line-height: 1.3`.
- **Фоновые кольца:** обводка осветлена до `rgba(54, 118, 67, 0.12)`.
- **Позиционирование:** реализовано равномерное JS-распределение 11 элементов по эллипсу (`centerX: 600`, `centerY: 520`, `radiusY: 280`, базовый `radiusX: 460` с расширением до 540 px в нижней части, чтобы длинные подписи не перекрывались). Для левой половины элементов текст разворачивается внутрь (`flex-direction: row-reverse`).
- **Адаптивность:** на планшете/мобильном (`<=1024 px`) inline-стили позиций сбрасываются, элементы выводятся сеткой/списком.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.3.8`.

## Последние правки (блок «Задачи клиентов» / «С чем к нам обращаются чаще всего»)

- **Зелёная карточка:** бейдж «Популярные задачи клиентов» теперь позиционирован у верхнего левого края (`top: 44 px`, `left: 44 px`), заголовок «С чем к нам обращаются чаще всего» прибит к нижнему левому краю (`bottom: 44 px`, `left: 44 px`).
- **Высоты блоков:** зелёная карточка и правый список выровнены по высоте (`min-height: 520 px`, список использует `justify-content: space-between`).
- **Иконки задач:** уменьшены с 40 px до 24 px (и до 24 px на мобильном).
- **Скругление карточек задач:** уменьшено с 12 px до 8 px.
- **Типографика заголовка:** `font-size: 36 px`, `font-weight: 700` (крупный акцентный заголовок в зелёной карточке).
- **Адаптивность:** на мобильном бейдж и заголовок сохраняют абсолютное позиционирование с отступами 24 px, высота списка сбрасывается в `auto`.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.3.9`.

## Последние правки (блок «Надёжный поставщик для медицинских учреждений региона»)

- **Заголовок секции:** теперь располагается на одной строке (`white-space: nowrap`), размер уменьшен до `40 px`, чтобы гарантированно помещаться в контейнере.
- **Сетка:** перестроена на три колонки в один ряд:
  - левая колонка — фото стоматологии с бейджами «20+ лет» и «5000+», растянута на обе строки (`grid-row: 1 / 3`);
  - центральная колонка — белый блок с текстом «Работа с государственными и частными учреждениями», растянута на обе строки;
  - правая колонка — фото склада («Склад в Чите») в первой строке и список преимуществ во второй строке.
- **Высоты колонок:** благодаря `align-items: stretch` левая и центральная колонки растягиваются до высоты правой колонки (склад + список), все три блока выровнены по верхнему краю.
- **Бейджи:** расположены внизу левой фото-карточки (`align-items: flex-end`), занимают всю ширину (`width: 100%`), растянуты через `flex: 1`.
- **Зелёный плюсик:** проверенное положение в правом верхнем углу белого блока (`top: 20 px`, `right: 20 px`), размер 24×24 px.
- **Текст в белом блоке:** оставлен `font-size: 18 px`, `font-weight: 600`, `line-height: 1.3` — компактный и читаемый.
- **Адаптивность:** на планшете (`<=1200 px`) и мобильном (`<=768 px`) сетка сбрасывается в поток с переносом заголовка (`white-space: normal`).
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.4.0`.

## Последние правки (блок «Реализованные проекты»)

- **Цвет заголовка:** убран зелёный класс, заголовок теперь чёрный (`var(--color-black)`), как в Figma.
- **Слайдер:** реализован рабочий кастомный слайдер на чистом JS:
  - добавлены стрелки «назад/вперёд» в виде зелёных круглых кнопок (`48×48 px`, `#3A7D44`) с белыми шевронами;
  - добавлены точки-индикаторы (`12×12 px`, зелёные) в правом нижнем углу карточки;
  - переключение по стрелкам и точкам с плавной анимацией (`opacity`/`visibility`, 0.4 s).
- **Структура карточки:** `.project-card` получил серый фон `#F5F5F5`, `border-radius: 16 px`, `padding: 40 px`; внутренний контент обёрнут в `.project-card-inner` с белым фоном, `border-radius: 12 px`.
- **Слайды:** сделано 2 fallback-слайда (Стоматология «Дента-Профи» и Медицинский центр «Забайкалье»), чтобы сразу демонстрировать переключение.
- **Номер слайда:** оставлен в верхнем левом углу белой области (`font-size: 12 px`, цвет текста `--color-black`).
- **Адаптивность:** на планшете и мобильном стрелки уменьшены/перенесены вниз, сетка карточки превращается в одну колонку.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.4.1`.

## Последние правки (блок «Работаем с ведущими производителями»)

- **Логотипы:** уменьшены с `214×120 px` до `90×90 px` (на мобильном — до `72×72 px`).
- **Карточки:** убрана фиксированная высота (`height: auto`, `min-height: unset`), padding уменьшен/сохранён компактным (`20 px` десктоп, `16 px` мобильный).
- **Отступы внутри карточек:** `margin-bottom` у логотипа и названия уменьшен до `12 px` / `8 px` соответственно.
- **Типографика:**
  - название производителя — `15 px` / `600` (на мобильном `14 px`);
  - описание — `12 px`, `line-height: 1.4`, цвет `#666` (на мобильном `11 px`).
- **Сетка:** `gap` между карточками уменьшен с `28 px` до `24 px`.
- **Скругление:** `border-radius: 8 px` сохранён.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.4.2`.

## Последние правки (финальный аудит макет vs сайт)

- **Круговая диаграмма (Supplies):**
  - убраны жёсткие CSS-позиции по `nth-child`;
  - JS-распределение переписано с чередованием двух радиусов (`radiusXInner: 360` / `radiusXOuter: 520`, `radiusYInner: 220` / `radiusYOuter: 320`), чтобы элементы выстроились по двум концентрическим эллипсам и не перекрывались;
  - центральное изображение увеличено с `304 px` до `352 px`;
  - фоновые кольца увеличены до `540/820/1080 px` и осветлены до `rgba(54, 118, 67, 0.15)`.
- **Audience («Работаем с учреждениями…»):**
  - `border-radius` карточек уменьшен до `8 px` на десктопе;
  - зелёный оверлей на фото-карточках усилен до `rgba(54, 118, 67, 0.6)`;
  - шрифт названия карточек уменьшен до `18 px`, чтобы уменьшить переносы.
- **FAQ:**
  - убрано разделение на две колонки в HTML, вопросы теперь идут в одном `.faq-grid`;
  - на десктопе каждый вопрос явно размещён по `grid-row`/`grid-column`, чтобы повторить макет Figma: левая колонка — 1, 4, 5; правая — 2, 3 (открытый);
  - открытый вопрос «Есть ли товары в наличии в Чите?» расположен в правой колонке второго ряда;
  - иконки заменены на шевроны вниз/вверх (∨/∧), размер увеличен до `12×8 px`.
- **Why («Надёжный поставщик»):**
  - заголовок дополнительно защищён от переноса (`display: inline-block`, `font-size: 36 px`);
  - зелёный плюсик уменьшен с `24 px` до `18 px`.
- **Hero:** описание остаётся `font-weight: 400`, иконки преимуществ — `24×24 px` (уже соответствовали макету).
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.4.3`.

## Последние правки (шапка — логотип)

- **Теглайн логотипа:** цвет изменён с чёрного на зелёный (`var(--color-green)`), как в макете Figma.
- **Примечание:** Figma API сейчас возвращает `429 Rate limit exceeded`, поэтому если логотип в макете отличается не только цветом теглайна, но и формой/иконкой — пришлите, пожалуйста, файл логотипа или его node ID из Figma, и я заменю его точь-в-точь.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.5.0`.

## Последние правки (FAQ — финальное соответствие макету Figma)

- **Расположение вопросов:** на десктопе воссоздано расположение из Figma:
  - левая колонка: «Какие дезсредства...», «Как подобрать рециркулятор?», «Какие средства подходят...»;
  - правая колонка: «Какие документы...», «Есть ли товары в наличии в Чите?» (открыт).
- **Реализация:** PHP вычисляет `grid-row`/`grid-column` для каждого вопроса; открытый вопрос всегда помещается в правую колонку своего ряда для баланса колонок.
- **Иконки:** шевроны вниз/вверх в виде SVG, размер `10×6 px`, толщина `1.5 px`.
- **Анимация:** добавлена плавная раскрытие/сворачивание ответа через `max-height`/`opacity`.
- **Адаптивность:** на мобильном `grid-row`/`grid-column` сбрасываются (`auto`), вопросы выстраиваются в одну колонку в исходном порядке.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.4.6`.

## Последние правки (Why — цвет заголовка)

- **Заголовок секции «Надёжный поставщик...»:** цвета перепутаны — теперь первая часть «Надёжный поставщик» зелёная (`text-green`), вторая часть «для медицинских учреждений региона» чёрная, как в Figma.
- **Реализация:** PHP оборачивает первые два слова заголовка в `<span class="text-green">`, остаток остаётся чёрным.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.4.7`.

## Последние правки (Why — бейджи, плюсик и центральный блок)

- **Заголовок секции:** между зелёной первой частью и чёрной второй частью добавлен принудительный перенос строки (`<br>`), размер шрифта уменьшен до `34 px`, чтобы вторая строка «для медицинских учреждений региона» целиком помещалась.
- **Бейджи «20+ лет» и «5000+»:**
  - контейнер `.why-stats` выровнен по верхнему краю (`align-items: flex-start`);
  - фиксированная высота бейджей увеличена до `100 px`;
  - запрещены переносы (`white-space: nowrap`);
  - размер числа уменьшен до `28 px`, подписи — `12 px`.
- **Центральный белый блок:** текст «Работа с государственными и частными учреждениями» прижат к низу (`align-items: flex-end`).
- **Плюсик:** увеличен до `36×36 px`, толщина линий `3 px`, цвет `#3A7D44`.
- **Список преимуществ (склад):** подложки стали компактнее — `padding: 12px 16px`, `border-radius: 8px`, `gap: 8px`, `max-width: 320px`.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.5.2`.

## Последние правки (блок «Партнёры»)

- **Логотипы карточек:** размер увеличен с `90×90 px` до `110×110 px`.
- **Вертикальные отступы карточек:** вертикальный `padding` уменьшен до `16px` (по горизонтали `24px`), чтобы карточки не казались слишком высокими; отступ от логотипа до названия — `12 px`, от названия до описания — `8 px`.
- **Заголовок секции:** добавлен принудительный перенос строки перед словом «производителями» (`<br>`), чтобы заголовок соответствовал двухстрочному макету Figma.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.5.3`.

## Последние правки (FAQ — отступ заголовка)

- **Отступ между заголовком «Часто задаваемые вопросы» и карточками FAQ:** уменьшен с `48 px` до `24 px` (`margin-bottom` у `.faq-section .section-header`), чтобы заголовок был визуально ближе к контенту, как в макете Figma.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.5.4`.

## Последние правки (FAQ — заголовок + описание)

- **Заголовок секции FAQ:** добавлено описание справа от заголовка (`faq_description`), которое выравнивается по правому краю и располагается на одном уровне с заголовком.
- **Вёрстка заголовка:** обёрнут в `.faq-header` с `display: flex`, `justify-content: space-between`, `align-items: flex-end`.
- **Отступ до карточек:** сохранён `margin-bottom: 24px` у `.faq-header`.
- **Адаптивность:** на мобильном описание переносится под заголовок и выравнивается по левому краю.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.5.5`.

## Последние правки (блок «Всё для организации инфекционного контроля» / Supplies)

- **Высота диаграммы:** увеличена с `700 px` до `950 px`, чтобы внешнее кольцо диаметром `900 px` полностью помещалось внутри белого контейнера.
- **Высота контейнера `.supplies-box`:** `min-height` увеличена до `1050 px`, нижний `padding` — до `100 px`.
- **Заголовок секции:** теперь две строки — «Все для организации» (чёрная) и «инфекционного контроля» (зелёная), каждая обёрнута в блок `span.supplies-title-line`.
- **Положение нижних элементов:**
  - «Упаковочные материалы для стерилизации» сдвинут левее/ниже (`bottom: 12%`, `left: 24%`).
  - «Рециркуляторы и обеззараживатели воздуха» сдвинут правее (`bottom: 12%`, `right: 26%`).
  - Для этих двух карточек добавлен `max-width: 180px` и перенос текста, чтобы длинные подписи не наезжали друг на друга.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.5.6`.

## Последние правки (CTA + футер)

- **CTA — иконка:** sparkle-звезда заменена на плюс (SVG с `stroke-linecap="round"`).
- **CTA — плюс:** вынесен за пределы `.application-info` и размещён в верхнем левом углу блока (`top: 24 px`, `left: 24 px`), толщина линий увеличена до `4 px`, непрозрачность `1`.
- **CTA — border-radius:** уменьшен с `20 px` до `14 px`.
- **CTA — чекбокс:** текст изменён с «Оставляя» на «Оставляю».
- **CTA — поле имени:** добавлены CSS-правила для скрытия системных иконок автозаполнения (`::-webkit-contacts-auto-fill-button`, `::-webkit-credentials-auto-fill-button`).
- **Футер — логотип:** файл `assets/img/logo-white.png` перекрашен в белый цвет, чтобы логотип и теглайн на зелёной карте были белыми.
- **CTA — заголовок:** добавлен принудительный перенос строки после слова «решение», чтобы получилось «Подберём решение / для вашего учреждения».
- **CTA — чекбокс:** текст согласия теперь в одну строку (`white-space: nowrap`, `font-size: 11px`), размер чекбокса уменьшен до `14×14 px`.
- **Футер — соцсети:** пустые белые квадраты заменены на иконки ВКонтакте (SVG) внутри `.footer-social`.
- **Футер — иконки без подложек:** убраны тёмные квадратные фоны, оставлены только белые символы ВК (`24×24 px`, `gap: 16 px`, выровнены по левому краю).
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.5.9`.

## Последние правки (блок «Что мы поставляем» / диаграмма)

- **Полностью переделано позиционирование элементов:** вместо JS-распределения по эллипсу теперь используется явное CSS-позиционирование каждого из 11 элементов внутри `.supplies-diagram`.
- **Порядок элементов (fallback) изменён** на соответствующий макету Figma:
  - правая дуга (внешний круг, 5 шт.): Дезсредства → Обработка поверхностей → Стерилизация инструментов → Контейнеры → Автоклавы;
  - левая дуга (средний круг, 4 шт.): Обработка рук → Салфетки → Антисептики → Эндоскопы;
  - нижний уровень (2 шт.): Упаковочные материалы → Рециркуляторы.
- **Направление элементов:** левые элементы используют `flex-direction: row-reverse`, чтобы точка-маркер оставалась ближе к центру, а текст — к краю.
- **Контейнер диаграммы:** добавлен `.supplies-diagram` размером `1100×700 px` с центром и кольцами по `50%`; фоновые кольца уменьшены до `420/660/900 px`, центральное изображение — `304×304 px` (как в Figma).
- **Адаптивность:** на планшете/мобильном диаграмма превращается в сетку/список, абсолютные позиции сбрасываются.
- **Кэшбастинг:** `TRIMED_VERSION` поднята до `1.5.1`.
